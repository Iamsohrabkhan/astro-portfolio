---
 const {posts}= Astro.props 
//  https://healthandwealthblogs.netlify.app
 ---

<section class="mt-12 pt-16 mx-auto px-4 max-w-screen-xl md:px-8 container dark:bg-dark dark:text-white">
	<div class="max-w-lg">
		<h1 class="text-3xl lg:text-5xl text-teal-500  font-semibold highlight w-fit px-4 py-2">
			Blogs
		</h1>
		<p class="mt-5 text-gray-500 bra animate-fade max-w-[15rem] md:max-w-md lg:max-w-xl coontainer mx-auto text-sm md:text-base dark:text-white">
			Blogs that are loved by the community. Updated every hour.
			The powerful gravity waves resulting from the impact of the planets, were finally resolved in 2015.
		</p>
	</div>

  <main class=" bg-white dark:bg-dark">
    <section class="px-4 mx-auto sm:px-6 lg:px-8 max-w-7xl">
        <div class="grid max-w-md grid-cols-1 mx-auto mt-12 lg:max-w-full lg:mt-16 lg:grid-cols-3 gap-x-16 gap-y-2 ">
          {
            posts.map((item,i)=>(            
              <article class="mb-2 animate-fade-up" style=`animation-delay: ${0.2*i}s;`>
                  <a href={`blogs/${item.slug}`} class="block aspect-w-4 aspect-h-3">
                      <img class="object-cover w-full h-72 rounded-lg" src={item.data.image} alt={item.data.title} />
                  </a>
                  <span class="sr-only inline-flex px-4 py-2 text-xs font-semibold tracking-widest uppercase rounded-full text-rose-500 bg-rose-100 mt-9  dark:text-white"> Technology </span>
                  <p class="mt-6 text-xl font-semibold  dark:text-white">
                      <a href={`blogs/${item.slug}`} class="text-black hover:text-teal-500 hover:underline  dark:text-white">{item.data.title}</a>
                  </p>
                  <p class="mt-4 text-gray-600 line-clamp-3  dark:text-white">{item.data.description}</p>
                  <span class="block text-sm font-bold tracking-widest text-gray-500 uppercase  dark:text-white"> Sohrab Khan. {item.data.date}</span>
                  <div class="h-0 mt-6 mb-4 border-t-2 border-gray-200 border-dashed  dark:border-white"></div>
              </article>
            ))
          }
        </div>
    </section>
</main>
</section>
<script>
	 import { annotate } from "https://unpkg.com/rough-notation?module";
  const e = document.querySelector(".highlight");
  const annotation2 = annotate(e, {
    animationDelay: 1000,
    type: "highlight",
    color: "#FFF176",
    padding: "50",
    animationDuration: 2000,
  });
  annotation2.show();
  const ev = document.querySelector(".bra");
  const annotation1 = annotate(ev, { type: 'bracket', color: 'red', padding: [2, 10], brackets: ['left', 'right'], strokeWidth: 3,  })
  annotation1.show();
</script>